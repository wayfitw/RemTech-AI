fastapi==0.115.6
uvicorn[standard]==0.34.0
anthropic==0.49.0
python-dotenv==1.0.1
PyJWT==2.10.1
python-multipart==0.0.20
python-docx==1.1.2
pypdf==5.1.0
openpyxl==3.1.5
python-pptx==1.0.2
reportlab==4.2.5
replicate==1.0.4
httpx==0.28.1
certifi==2026.6.17
trafilatura==2.0.0

# Целевой стек БД (EPIC-01: PostgreSQL + pgvector)
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0
pydantic-settings==2.7.1
pgvector==0.3.6

# Очереди/фон (EPIC-03: конвейер ингеста базы знаний) + Redis-состояние оркестратора
celery[redis]==5.4.0
redis==5.2.1


# Голосовой ввод (EPIC-10, issue #34) — локальный STT на CTranslate2, CPU/GPU.
# Модель качается при первом запуске; голос не покидает контур (ADR-010/011).
# av 18 — abi3-wheel (Python 3.13); faster-whisper 1.2 снял верхний пин на av.
av==18.0.0
faster-whisper==1.2.1

# Голосовые ответы (EPIC-10, issue #40) — локальный TTS Silero через torch.hub.
# CPU-версия torch; модель качается при первом синтезе; голос не покидает контур.
torch>=2.6
omegaconf>=2.3   # конфиг для Silero TTS (torch.hub)
telethon==1.44.0
